import matplotlib.pyplot as plt
from src.grouping.i_grouping_algo import I_Grouping_Algo

class Brute_Force(I_Grouping_Algo):

    def group(self, vector_list: list) -> None:
        """
            vector: point, dimensions: x, y, z
        """
        # 1. calculate group loads and selfcons.
        {frozenset()}
        # 2. combine group autarky to area autarky
    def animate(self, ax :plt.Axes):
        """Show an animatoin of the algo"""
        